export type ArticleProps = {
  id: string;
  title: string;
  section?: string;
  date?: string;
  image: string;
};

export type DataContact = {
  name?: string;
  email?: string;
  message?: string;
};
